import 'package:flutter/material.dart';

class suprimentos {
  //cores para usar no aplicativo... no caso são atributos.
  Color rosaClaro = const Color.fromRGBO(255, 213, 255, 1);
  Color rosaEscuro = const Color.fromRGBO(210, 35, 207, 1);
  Color azulEscuro = const Color.fromARGB(255, 38, 108, 178);
  Color azulClaro = const Color.fromARGB(255, 115, 180, 246);
  
}